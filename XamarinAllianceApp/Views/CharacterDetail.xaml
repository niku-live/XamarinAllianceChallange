<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinAllianceApp.Views.CharacterDetail">
    <ContentPage.Content>
        <StackLayout>
            <Label Text="More about character" />
            <Label Text="{Binding Item.Name}" FontAttributes="Bold" />
            <Label Text="{Binding Item.Biography}" />
            <Label Text="{Binding Item.Gender}" />
            <Label Text="{Binding Item.Height}" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>